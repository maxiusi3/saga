import { Router } from 'express';

const router = Router();

// Placeholder for dashboard routes - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Dashboard routes - coming soon' });
});

export default router;