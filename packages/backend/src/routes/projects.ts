import { Router } from 'express';

const router = Router();

// Placeholder for project routes - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Project routes - coming soon' });
});

export default router;