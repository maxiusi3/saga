'use client'

import { SettingsPage } from '@/components/settings/settings-page'

export default function DashboardSettingsPage() {
  // SettingsPage component handles all data fetching and API calls internally
  return <SettingsPage />
}
